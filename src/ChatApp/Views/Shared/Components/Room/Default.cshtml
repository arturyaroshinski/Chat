@model IEnumerable<Chat>

<div class="sidebar">
    @foreach (var chat in Model)
    {
        <a class="room-button" asp-controller="Home" asp-action="Chat" asp-route-id="@chat.Id">@chat.Name</a>
    }
    <div class="room-button create-room-btn"><strong>+</strong></div>
</div>